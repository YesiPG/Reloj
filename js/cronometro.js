(function(){
    "use strict"

    const d = document;
    const crH = d.querySelector(".crH");
    const crM = d.querySelector(".crM");
    const crS = d.querySelector(".crS");

    d.addEventListener("click",(e)=>{

        if(e.target.matches(".btnCrReset")){

        }

        if(e.target.matches(".btnCrIniciar")){

        }
    })
})();